#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print character from a to z
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	char chr = 'a';

	while (chr <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
