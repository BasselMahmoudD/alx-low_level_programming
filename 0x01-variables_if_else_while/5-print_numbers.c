#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print from 0 to 9
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%i\n", n);
		n++;
	}
	return (0);
}
